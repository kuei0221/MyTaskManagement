<h1>Edit This Mission</h1>

<%= form_for(@mission, url: mission_path(@mission.id), method: "patch") do |f| %>
  
<%= f.label :name %>
<%= f.text_field :name %>

<%= f.label :content %>
<%= f.text_area :content %>

<%= f.submit "Update!" %>
<% end %>


<%= link_to "Cancel and Back to Missions list", missions_path %>